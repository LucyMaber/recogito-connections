// Compatibility shim removed — this project now assumes upstream serialized
// W3C-style annotation objects (with `target` array and `body` array).
//
// If some code still imports this module, keep this explicit (non-breaking)
// placeholder so tooling errors are clear.

// NOTE: This file intentionally no longer implements a local adapter. Use
// upstream-serialized objects (plain JS) instead of a wrapper.

export default undefined;
